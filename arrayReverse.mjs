export default function arrayreverse(){
    console.log('-------------------------------------');
    console.log('reverse  --- reverse an array');
    const number = ['one','two', 'three'];
    console.log(number);
    const reversed = number.reverse();
    console.log(reversed);
}